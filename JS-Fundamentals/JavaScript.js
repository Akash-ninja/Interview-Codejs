// JS types - 7 primitives

/*  undefined
    Boolean
    Number 
    String 
    BigInt 
    Symbol */

    // null : typeof instance === "object". 
    // Special primitive type having additional usage for it's value: if object is not inherited, then null is shown;

    // Object : typeof instance === "object". Special non-data but structural
    // type for any constructed object instance also used as data structures: new Object, new Array, new Map, new Set, new WeakMap, new WeakSet, new Date and almost everything made with new keyword.